/**
 * Utilisez des opérateurs bit à bit pour inverser les bits d'un nombre entier.
 * Affichez le résultat en binaire.
 */

function inverserBits (nombre) {
    let entier = nombre;
    console.log(entier.toString(2));
}

let a = 45;
inverserBits(a);